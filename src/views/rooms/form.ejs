<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title><%= room ? 'Editar Sala' : 'Nova Sala' %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <h1><%= room ? 'Editar Sala' : 'Cadastrar Sala' %></h1>
  <form id="roomForm">
    <input type="hidden" id="id" value="<%= room ? room.id : '' %>">
    
    <label for="numero_sala">Número da Sala:</label>
    <input type="number" id="numero_sala" value="<%= room ? room.numero_sala : '' %>" required>
    <br>
    <label for="capacidade_maxima">Capacidade Máxima:</label>
    <input type="number" id="capacidade_maxima" value="<%= room ? room.capacidade_maxima : '' %>" required>
    <br>
    <button type="submit">Salvar</button>
  </form>
  <a href="/rooms">Voltar</a>

  <script src="/js/roomsForm.js"></script> </body>
</html>